const lang = ["c++", "java", "go", "typescript", "javascript", "python", "css"];

for( languages of lang){
    console.log(languages);
}

// the lang elements will be stored in languages one by one

for(char of "apnacollage"){
    console.log(char);
}